#include <iostream>
using  namespace std;
int exp3 (int a, int b){
    if (b==0)
    return 1;
    else if (b%2 == 0)
    int  x  = exp3(a*a,b/2);
    return x;
    else {
        return   a x exp3(a,b *1)
    }
    
}
